{% extends 'base.html.twig' %}

{% block title %}Accueil - Tournoi Masterchef{% endblock %}

{% block stylesheets %}
    <style>
        body {
            background-color: #1f1f1f;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }

        .hero-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            height: 100vh;
            background: radial-gradient(circle at center, #2c2c2c 0%, #111 100%);
        }

        .hero-container h1 {
            font-size: 3rem;
            margin-bottom: 2rem;
            color: #e63946;
            text-shadow: 2px 2px 8px #000;
        }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 1rem;
        }

        .btn {
            padding: 0.8rem 2.4rem;
            font-size: 1.3rem;
            font-weight: 600;
            border: none;
            border-radius: 50px;
            transition: transform 0.2s ease, background-color 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
            cursor: pointer;
            text-decoration: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, #ff4e50, #f9d423);
            color: #2c3e50;
        }

        .btn-primary:hover {
            background-color: #f1b500;
            transform: translateY(-4px);
            box-shadow: 0 12px 18px rgba(0, 0, 0, 0.3);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #f9d423, #ff4e50);
            color: #2c3e50;
        }

        .btn-secondary:hover {
            background-color: #2471a3;
            transform: translateY(-4px);
            box-shadow: 0 12px 18px rgba(0, 0, 0, 0.3);
        }

        .hero-image {
            text-align: center;
            margin-top: 3rem;
            animation: fadeIn 2s ease-out;
        }

        .hero-image img {
            max-width: 75%;
            height: auto;
            border-radius: 15px;
            box-shadow: 0 0 25px rgba(255, 200, 0, 0.3);
            transition: transform 0.4s ease;
            background: linear-gradient(135deg, #1c1c1c, #2b2b2b);
            /*mix-blend-mode: lighten;*/
        }

        .hero-image img:hover {
            transform: scale(1.02);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

    </style>
{% endblock %}

{% block body %}
    <div class="hero-container">
        <h1>🔥 Bienvenue sur le site de gestion de tournois ! 🔥<br><small>Prépare-toi, ça va chauffer sur le terrain…</small></h1>
        <div class="btn-container">
            <a href="{{ path('app_login') }}" class="btn btn-primary">Se connecter</a>
            <a href="{{ path('app_register') }}" class="btn btn-secondary">S'inscrire</a>
        </div>
        <div class="hero-image">
            <img src="{{ asset('assets/versus_sportif.png') }}" alt="Combat VS" />
        </div>

    </div>
{% endblock %}
